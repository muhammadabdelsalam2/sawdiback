@extends('layouts.customer.dashboard')
@section('title', __('farms.titles.farms'))
@push('styles')<link rel="stylesheet" href="{{ asset('assets/css/pages/livestock.css') }}">@endpush
@section('content')
<div class="container py-4 livestock-page"><div class="page-head"><h2 class="page-title">{{ __('farms.titles.farms') }}</h2><a class="btn btn-primary-green" href="{{ route('customer.farms.create', ['locale' => $currentLocale]) }}">{{ __('farms.actions.add_farm') }}</a></div>@include('dashboard.customer.farms.partials.flash')<div class="table-container"><table class="table registry-table mb-0"><thead><tr><th>{{ __('farms.fields.name') }}</th><th>{{ __('farms.fields.type') }}</th><th>{{ __('farms.fields.location') }}</th><th>{{ __('farms.fields.pens_count') }}</th><th>{{ __('farms.fields.status') }}</th><th>{{ __('farms.fields.actions') }}</th></tr></thead><tbody>@forelse($farms as $farm)<tr><td>{{ $farm->name }}</td><td>{{ __('farms.options.' . $farm->type) }}</td><td>{{ $farm->location ?? '-' }}</td><td>{{ $farm->pens_count }}</td><td>{{ $farm->is_active ? __('farms.options.active') : __('farms.options.inactive') }}</td><td><a class="btn btn-sm btn-outline-secondary" href="{{ route('customer.farms.edit', ['locale' => $currentLocale, 'farm' => $farm->id]) }}">{{ __('farms.actions.edit') }}</a></td></tr>@empty<tr><td colspan="6">{{ __('farms.empty.no_farms') }}</td></tr>@endforelse</tbody></table>{{ $farms->links('pagination::bootstrap-5') }}</div></div>
@endsection
