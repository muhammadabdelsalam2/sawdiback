@extends('layouts.customer.dashboard')
@section('title', __('farms.titles.pens'))
@push('styles')<link rel="stylesheet" href="{{ asset('assets/css/pages/livestock.css') }}">@endpush
@section('content')
<div class="container py-4 livestock-page"><div class="page-head"><h2 class="page-title">{{ __('farms.titles.pens') }}</h2><a class="btn btn-primary-green" href="{{ route('customer.farm-pens.create', ['locale' => $currentLocale]) }}">{{ __('farms.actions.add_pen') }}</a></div>@include('dashboard.customer.farms.partials.flash')<div class="table-container"><table class="table registry-table mb-0"><thead><tr><th>{{ __('farms.fields.pen_number') }}</th><th>{{ __('farms.fields.farm') }}</th><th>{{ __('farms.fields.type') }}</th><th>{{ __('farms.fields.animal_count') }}</th><th>{{ __('farms.fields.net_profit') }}</th><th>{{ __('farms.fields.actions') }}</th></tr></thead><tbody>@forelse($pens as $pen)<tr><td>{{ $pen->pen_number }}</td><td>{{ $pen->farm?->name }}</td><td>{{ __('farms.options.' . $pen->type) }}</td><td>{{ $pen->animal_count }}</td><td>{{ $pen->net_profit }}</td><td class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('customer.farm-pens.show', ['locale' => $currentLocale, 'farm_pen' => $pen->id]) }}">{{ __('farms.actions.view') }}</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('customer.farm-pens.edit', ['locale' => $currentLocale, 'farm_pen' => $pen->id]) }}">{{ __('farms.actions.edit') }}</a></td></tr>@empty<tr><td colspan="6">{{ __('farms.empty.no_pens') }}</td></tr>@endforelse</tbody></table>{{ $pens->links('pagination::bootstrap-5') }}</div></div>
@endsection
