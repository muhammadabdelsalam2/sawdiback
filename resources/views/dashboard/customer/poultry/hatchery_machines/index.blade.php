@extends('layouts.customer.dashboard')
@section('title', __('poultry.titles.hatchery_machines'))
@push('styles')<link rel="stylesheet" href="{{ asset('assets/css/pages/livestock.css') }}">@endpush
@section('content')
<div class="container py-4 livestock-page"><div class="page-head"><h2 class="page-title">{{ __('poultry.titles.hatchery_machines') }}</h2><a class="btn btn-primary-green" href="{{ route('customer.poultry.hatchery-machines.create', ['locale' => $currentLocale]) }}">{{ __('poultry.actions.add_hatchery_machine') }}</a></div>@include('dashboard.customer.poultry.partials.flash')<div class="table-container"><table class="table registry-table mb-0"><thead><tr><th>{{ __('poultry.fields.machine_number') }}</th><th>{{ __('poultry.fields.farm') }}</th><th>{{ __('poultry.fields.capacity') }}</th><th>{{ __('poultry.fields.batches_count') }}</th><th>{{ __('poultry.fields.status') }}</th><th>{{ __('poultry.fields.actions') }}</th></tr></thead><tbody>@forelse($machines as $machine)<tr><td>{{ $machine->machine_number }}</td><td>{{ $machine->farm?->name ?? '-' }}</td><td>{{ $machine->capacity }}</td><td>{{ $machine->batches_count }}</td><td>{{ $machine->is_active ? __('poultry.options.active') : __('poultry.options.inactive') }}</td><td><a class="btn btn-sm btn-outline-secondary" href="{{ route('customer.poultry.hatchery-machines.edit', ['locale' => $currentLocale, 'hatchery_machine' => $machine->id]) }}">{{ __('poultry.actions.edit') }}</a></td></tr>@empty<tr><td colspan="6">{{ __('poultry.empty.no_hatchery_machines') }}</td></tr>@endforelse</tbody></table>{{ $machines->links('pagination::bootstrap-5') }}</div></div>
@endsection
