@extends('layouts.customer.dashboard')
@section('title', __('poultry.titles.hatchery_batches'))
@push('styles')<link rel="stylesheet" href="{{ asset('assets/css/pages/livestock.css') }}">@endpush
@section('content')
<div class="container py-4 livestock-page"><div class="page-head"><h2 class="page-title">{{ __('poultry.titles.hatchery_batches') }}</h2><a class="btn btn-primary-green" href="{{ route('customer.poultry.hatchery-batches.create', ['locale' => $currentLocale]) }}">{{ __('poultry.actions.add_hatchery_batch') }}</a></div>@include('dashboard.customer.poultry.partials.flash')<div class="table-container"><table class="table registry-table mb-0"><thead><tr><th>{{ __('poultry.fields.batch_number') }}</th><th>{{ __('poultry.fields.machine') }}</th><th>{{ __('poultry.fields.eggs_loaded') }}</th><th>{{ __('poultry.fields.chicks_produced') }}</th><th>{{ __('poultry.fields.success_rate') }}</th><th>{{ __('poultry.fields.actions') }}</th></tr></thead><tbody>@forelse($batches as $batch)<tr><td>{{ $batch->batch_number }}</td><td>{{ $batch->machine?->machine_number }}</td><td>{{ $batch->eggs_loaded }}</td><td>{{ $batch->chicks_produced }}</td><td>{{ $batch->success_rate }}%</td><td class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('customer.poultry.hatchery-batches.show', ['locale' => $currentLocale, 'hatchery_batch' => $batch->id]) }}">{{ __('poultry.actions.view') }}</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('customer.poultry.hatchery-batches.edit', ['locale' => $currentLocale, 'hatchery_batch' => $batch->id]) }}">{{ __('poultry.actions.edit') }}</a></td></tr>@empty<tr><td colspan="6">{{ __('poultry.empty.no_hatchery_batches') }}</td></tr>@endforelse</tbody></table>{{ $batches->links('pagination::bootstrap-5') }}</div></div>
@endsection
